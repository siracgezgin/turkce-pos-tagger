# ==============================================================================
# Türkçe POS Tagging Sistemi - Proje Bağımlılıkları
# ==============================================================================
#
# Bu dosya, projenin çalışması ve geliştirilmesi için gereken tüm Python
# kütüphanelerini listeler. Versiyonların sabitlenmesi (`==`), projenin farklı
# ortamlarda tutarlı bir şekilde çalışmasını sağlar.
#
# Kurulum için aşağıdaki komutu bir sanal ortam (virtual environment)
# içerisinde çalıştırın:
# pip install -r requirements.txt
#
# ------------------------------------------------------------------------------

# == 1. Çekirdek Bilimsel Hesaplama ve Veri İşleme ==
# Bu kütüphaneler, projenin temelindeki sayısal işlemler ve veri manipülasyonu
# için gereklidir.
# ------------------------------------------------------------------------------
numpy==1.26.4         # Python'da bilimsel hesaplama için temel paket (matrisler, diziler).
scipy==1.13.0         # Bilimsel ve teknik hesaplama için temel algoritmalar.
pandas==2.2.2         # Veri analizi ve manipülasyonu için güçlü bir araç (CSV, CoNLL-U okuma vb.).

# == 2. Makine Öğrenmesi ve İstatistiksel Modeller ==
# Sistemin merkezindeki CRF modeli ve diğer klasik ML algoritmaları için.
# ------------------------------------------------------------------------------
scikit-learn==1.4.2   # Kapsamlı makine öğrenmesi araçları (metrikler, modeller).
sklearn-crfsuite==0.3.6 # Projemizin temel taşı olan CRF modelinin entegrasyonu.

# == 3. Doğal Dil İşleme (NLP) Kütüphaneleri ==
# Hem temel NLP işlemleri hem de karşılaştırma ve deneyler için.
# ------------------------------------------------------------------------------
nltk==3.8.1           # NLP için temel bir kütüphane (tokenizasyon, n-gramlar vb.).
stanza==1.8.2         # Stanford'un sinir ağı tabanlı, Türkçe destekli NLP pipeline'ı.
spacy==3.7.4          # Endüstri standardı, üretim odaklı bir diğer NLP kütüphanesi.
# Spacy için Türkçe modelini ayrıca yüklemeniz gerekir:
# python -m spacy download tr_core_news_sm

# == 4. Derin Öğrenme ve Transformer Mimarileri (Deneysel Çalışmalar İçin) ==
# Raporda bahsedilen BERT gibi modern yaklaşımlarla deneyler yapmak için.
# ------------------------------------------------------------------------------
torch==2.3.0          # PyTorch: Esnek ve güçlü bir derin öğrenme çatısı.
transformers==4.41.2  # Hugging Face kütüphanesi (BERTurk gibi modellere erişim).
datasets==2.19.0      # Hugging Face'in veri seti yönetimi kütüphanesi.
accelerate==0.30.1    # PyTorch eğitimini hızlandırmak ve kolaylaştırmak için.

# == 5. Geliştirme Ortamı ve Veri Görselleştirme ==
# `notebooks` klasöründeki deneysel analizler ve görselleştirmeler için.
# ------------------------------------------------------------------------------
jupyterlab==4.2.1     # Jupyter Notebook'lar için gelişmiş bir web arayüzü.
matplotlib==3.8.4     # 2D grafikler ve çizimler için temel kütüphane.
seaborn==0.13.2       # Matplotlib tabanlı, istatistiksel veri görselleştirme kütüphanesi.
tqdm==4.66.4          # Uzun süren döngüler ve işlemler için akıllı bir ilerleme çubuğu.

# == 6. Kod Kalitesi, Formatlama ve Test ==
# Profesyonel yazılım geliştirme standartlarını uygulamak için.
# ------------------------------------------------------------------------------
black==24.4.2         # Kodun otomatik olarak standart bir formatta düzenlenmesini sağlar.
flake8==7.0.0         # Kod stilini (PEP8) ve olası hataları denetleyen bir araç (linter).
pytest==8.2.0         # Python için standart test çatısı.

# == 7. API Sunumu ve Dağıtım (Deployment) ==
# Modeli bir web servisi olarak sunmak için gerekli kütüphaneler.
# ------------------------------------------------------------------------------
fastapi==0.111.0      # Yüksek performanslı, modern Python web çatısı.
uvicorn[standard]==0.29.0 # FastAPI uygulamalarını çalıştırmak için bir ASGI sunucusu.
pydantic==2.7.1       # API için veri doğrulama ve ayar yönetimi.